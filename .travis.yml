before_install:
  - sudo apt-get install cmake aspectc++ -y
before_script: ./travis-ci/configure.sh
script: ./travis-ci/make.sh
language: cpp
compiler:
  - g++
branches:
  only:
    - master
notifications:
  email:
    on_success: change
    on_failure: always