Things to do:
=============
 * Simple Routing for IPv4/v6 if multiple interfaces
 * IPv4: kein tcp/udp erlauben, wenn noch keine src addresse. Wie bei ipv6 sollte beim
         interface->up() eine adresse zugewiesen werden oder up() verhindert werden.
         (DHCP)
 * TCP: Incoming packets should not be able to steal buffers for acknowledgements in TCP
   -> Receiving window < available buffers
 * TCP: NODELAY ausschalten/einschalten des Nagle Algorithmus zur Verhinderung von zu kleinen
        Paketen beim absenden.
 * IGMP